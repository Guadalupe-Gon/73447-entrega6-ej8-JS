const edad = [18, 25, 14, 32, 16, 40];

edad.forEach((edades) => {

    if (edades >= 18) {

        console.log(`Edad: ${edades} - Mayor de edad`);

    } else {

        console.log(`Edad: ${edades} - Menor de edad`);

    }

})